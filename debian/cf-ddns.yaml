api_token: "secretkey"
zone: "example.com"

## full domain name: defaults to <hostname>.<zone>
# domain: "example.example.com"

## enable A record registration, default to true
# ipv4: false

## enable AAAA record registration, default to true
# ipv6: false

## A/AAAA record ttl, default to 600(10 minutes)
# ttl = 600
